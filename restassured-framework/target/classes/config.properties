# ============================================================
# Application Configuration - QA Environment
# This file contains all environment-specific configurations.
# Switch environments by using -Denv=qa / uat / prod in Maven.
# ============================================================

# --- Base URL ---
base.url=https://reqres.in

# --- API Endpoints ---
endpoint.users=/api/users
endpoint.auth=/api/login
endpoint.register=/api/register

# --- Authentication ---
# These credentials are used by AuthService to get a token
auth.email=eve.holt@reqres.in
auth.password=cityslicka

# --- Timeouts (milliseconds) ---
connection.timeout=10000
read.timeout=30000

# --- Default Test Data ---
default.page=1
default.per.page=6
